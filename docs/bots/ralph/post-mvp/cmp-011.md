# CMP-011: Deterministic party combat commands

- Epic: KAP-543
- Depends on: CMP-010, MVP-003
- Status: blocked by dependency

```gherkin
Scenario: Execute an owner-selected party action
Given an owned companion in the owner's party
When the owner issues assist, defend, or attack-marked-target
Then the controller selects only a legal hostile target
And normal movement and combat systems execute the action

Scenario: Stop or reject an invalid action
Given a stale goal, foreign command, friendly target, missing target or
unreachable target
When the command reaches the world thread
Then it is rejected and cannot resume after stop
```

Add typed commands and a small observation -> trigger -> action boundary.
Keep role selection and LLM transport outside this slice.
