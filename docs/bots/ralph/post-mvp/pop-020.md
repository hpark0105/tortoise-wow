# POP-020: Independent regional director and observability

- Epic: KAP-543
- Depends on: TW-013
- Status: ready after lower-priority companion cards

```gherkin
Scenario: Maintain activity away from the human
Given persistent bots assigned across at least six zones
When the human remains in one zone for sixty minutes
Then configured remote-zone floors remain active
And movement or meaningful gameplay succeeds in three human-free zones

Scenario: Degrade under load without hiding it
Given world processing exceeds a configured threshold
When the director reduces optional work
Then new logins pause first
And companions, combat and a declared remote activity floor remain active
And status reports requested, active, reduced-detail and parked counts
```

Implement assignments, gradual pacing and read-only status first. Remote
progress must come from normal actions; do not fabricate offline rewards or
relocate bots when the player changes zones.
